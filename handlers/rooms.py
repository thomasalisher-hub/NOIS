# handlers/rooms.py
from aiogram import Router

room_router = Router()

def setup_room_handlers(router, db, chat_manager):
    """Настройка обработчиков комнат"""
    # Заглушка - реализуем на следующем шаге
    pass